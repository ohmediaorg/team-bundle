{% extends '@OHMediaBackend/form.html.twig' %}

{% set form_title = 'Edit Team Member' %}

{% block breadcrumbs %}
{{ bootstrap_breadcrumbs(
  bootstrap_breadcrumb(bootstrap_icon('microsoft-teams') ~ ' Teams', 'team_index'),
  bootstrap_breadcrumb(team, 'team_view', {id: team.id}),
  bootstrap_breadcrumb('Edit Member'),
) }}
{% endblock %}
